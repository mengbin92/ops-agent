# 运维 Agent (ops-agent) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 构建一个默认只读、高风险操作需会话内授权并附风险说明与快照回滚的 Claude Code 运维子代理。

**Architecture:** 三个文件落地——`.claude/agents/ops.md`（Agent 定义与流程纪律）、`opsx`+`core.py`（纯标准库 CLI：分类/审批戳/快照/回滚/审计）、`.claude/settings.json`（PreToolUse hook 注册）。hook 对所有 Bash 命令执行 `opsx check`，只读白名单或有效审批戳放行，否则拒绝。core.py 为纯函数库，是将来 MCP 演进的接口边界。

**Tech Stack:** python3，纯标准库（json/hashlib/shutil/subprocess/argparse/pathlib/re/datetime/uuid/time/tempfile/stat）。无第三方依赖、无测试框架（assert 自检）。

**Spec:** `docs/superpowers/specs/2026-09-19-ops-agent-design.md`

## Global Constraints

- 只使用 python3 标准库，不引入任何第三方包（spec 第 5 节）。
- 状态目录：`~/.ops-agent/`，可用环境变量 `OPSX_STATE_DIR` 覆盖（测试必须用它指向临时目录）。
- 命令规范化 = 压缩空白；审批戳哈希 = sha256(规范化命令)。
- 审批戳默认 TTL 900 秒；R3 命令必须 `--force` 才发戳；R0 命令不可发戳。
- 复合命令按 `;` `&&` `||` `|` 分段，取最高风险级（spec 3.1）。
- 未匹配任何规则的命令默认 R2（安全方向默认）。
- 快照保留最近 200 个，超出按时间清理（spec 4.5）。
- 审计日志为 JSONL：`~/.ops-agent/audit.jsonl`，每行必须可 `json.loads`。
- `opsx exec` 快照失败必须中止执行（spec 9）。
- hook 入口任何异常 → exit 2（拒绝方向，spec 9）。
- git 提交信息不包含任何 Claude/AI 辅助署名（用户全局规则）。
- 唯一硬性质量门：`python3 selftest.py` 全绿。

---

### Task 1: 配置加载与命令分类（core.py 骨架 + config.yaml）

**Files:**
- Create: `config.yaml`
- Create: `core.py`
- Create: `selftest.py`

**Interfaces:**
- Produces:
  - `classify(cmd: str) -> tuple[str, str | None]`（返回 `(级别, 命中的分段)`，级别为 `"R0"|"R1"|"R2"|"R3"`）
  - `load_config() -> dict`，结构 `{"readonly_patterns": [...], "risk_rules": {"R1": [...], "R2": [...], "R3": [...]}}`
  - `STATE_DIR: Path`（来自 `OPSX_STATE_DIR` 环境变量或 `~/.ops-agent`）
  - `OpsxError(Exception)`

- [ ] **Step 1: 写 config.yaml**

```yaml
# 只读白名单：命中即 R0 直接放行（按顺序匹配）
readonly_patterns:
  - ^kubectl\s+(get|describe|logs|top|api-resources|config\s+view)\b
  - ^systemctl\s+(status|list-units|list-timers|show|is-active|is-enabled|list-dependencies)\b
  - ^journalctl\b
  - ^docker\s+(ps|images|logs|inspect|stats|port|top|version|info)\b
  - ^docker\s+compose\s+(ps|logs|config|top)\b
  - ^(cat|less|head|tail|grep|egrep|ls|find|ps|top|htop|df|du|free|uname|hostname|date|uptime|env|which|whoami|id)\b
  - ^ssh\s+\S+\s+"(systemctl\s+status|journalctl|docker\s+ps|kubectl\s+get|cat|tail|grep)
  - ^(brew\s+(list|info|services|outdated)|launchctl\s+list)\b

risk_rules:
  R1:
    - ^systemctl\s+(restart|reload)\b
    - ^docker\s+(restart|stop|start)\b
    - ^docker\s+compose\s+(restart|up|stop|start)\b
    - ^brew\s+(install|upgrade|services\s+start)\b
    - ^launchctl\s+(load|unload|kickstart)\b
  R2:
    - ^kubectl\s+(apply|rollout|scale|set|patch|create|replace|edit|taint|cordon|uncordon)\b
    - ^systemctl\s+(stop|disable|enable|mask)\b
    - ^apt(-get)?\s+(install|remove|upgrade)\b
    - ^yum\s+(install|remove|update)\b
    - ^brew\s+(uninstall|services\s+(stop|restart))\b
    - ^docker\s+(build|push|pull|run)\b
    - ^docker\s+compose\s+(build|pull|down)\b
    - ^(iptables|nft)\b
  R3:
    - ^rm\s+(-\S+\s+)*-rf\b
    - ^rm\s+(-\S+\s+)*-r\b
    - \bmkfs(\.\w+)?\b
    - ^kubectl\s+delete\b
    - ^docker\s+(rm|rmi|system\s+prune|volume\s+rm)\b
    - \bDROP\s+(TABLE|DATABASE)\b
    - ^dd\s+.*of=/dev/
```

- [ ] **Step 2: 写失败测试 selftest.py**

```python
"""opsx 自检：python3 selftest.py，全部 assert，无框架。"""
import os
import tempfile

os.environ["OPSX_STATE_DIR"] = tempfile.mkdtemp(prefix="opsx-test-")

from pathlib import Path  # noqa: E402
import json  # noqa: E402

import core  # noqa: E402


def test_classify():
    assert core.classify("kubectl get pods -n default")[0] == "R0"
    assert core.classify("systemctl restart nginx")[0] == "R1"
    assert core.classify("kubectl apply -f deploy.yaml")[0] == "R2"
    assert core.classify("kubectl delete pod x")[0] == "R3"
    assert core.classify("docker ps && rm -rf /tmp/x")[0] == "R3"
    assert core.classify("some-unknown-write")[0] == "R2"


def test_audit():
    core.audit("test", foo=1)
    line = Path(core.STATE_DIR, "audit.jsonl").read_text().strip().splitlines()[-1]
    rec = json.loads(line)
    assert rec["event"] == "test" and rec["foo"] == 1


def test_stamp():
    core.approve("systemctl restart nginx", ttl_seconds=60)
    assert core.check_stamp("systemctl restart nginx") is True
    core.approve("systemctl restart oldsvc", ttl_seconds=60)
    p = Path(core.STATE_DIR, "approvals", core.cmd_hash("systemctl restart oldsvc") + ".json")
    s = json.loads(p.read_text())
    s["created"] -= 3600
    p.write_text(json.dumps(s))
    assert core.check_stamp("systemctl restart oldsvc") is False
    try:
        core.approve("rm -rf /tmp/x")
        raise AssertionError("R3 无 --force 应拒绝发戳")
    except core.OpsxError:
        pass


def test_check():
    assert core.check("docker ps")[0] == 0
    code, msg = core.check("systemctl restart nginx")
    assert code == 2 and "变更单" in msg


def test_snapshot_file_rollback():
    import tempfile as tf
    with tf.TemporaryDirectory() as td:
        f = Path(td) / "app.conf"
        f.write_text("v1")
        sid = core.snapshot_file(str(f))
        f.write_text("v2")
        core.rollback(sid)
        assert f.read_text() == "v1"


def test_snapshot_cmd():
    sid = core.snapshot_cmd("echo hello-opsx", name="greeting")
    report = core.rollback(sid)
    assert "hello-opsx" in report


def test_exec_aborts_on_snapshot_failure():
    core.approve("echo hi", ttl_seconds=60)
    code = core.exec_change("echo hi", snapshot_files=["/nonexistent/path/xyz"])
    assert code != 0


def test_exec_happy():
    import tempfile as tf
    with tf.TemporaryDirectory() as td:
        f = Path(td) / "a.txt"
        f.write_text("before")
        cmd = f"echo after > {f}"
        core.approve(cmd, ttl_seconds=60)
        code = core.exec_change(cmd, snapshot_files=[str(f)])
        assert code == 0 and f.read_text() == "after"


ALL = [v for k, v in sorted(globals().items()) if k.startswith("test_")]


def main():
    failed = 0
    for fn in ALL:
        try:
            fn()
            print(f"PASS {fn.__name__}")
        except Exception as e:  # noqa: BLE001
            failed += 1
            print(f"FAIL {fn.__name__}: {e}")
    print(f"{len(ALL) - failed}/{len(ALL)} passed")
    return 1 if failed else 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 3: 运行测试确认失败**

Run: `cd ops-agent && python3 selftest.py`
Expected: FAIL（`ModuleNotFoundError: No module named 'core'`）

- [ ] **Step 4: 写 core.py（本任务部分：配置 + 分类）**

```python
"""opsx 核心库：纯函数，CLI 与未来 MCP server 共用。"""
import hashlib
import json
import os
import re
import shutil
import stat
import subprocess
import sys
import time
import uuid
from datetime import datetime, timezone
from pathlib import Path

STATE_DIR = Path(os.environ.get("OPSX_STATE_DIR", str(Path.home() / ".ops-agent")))
REPO_DIR = Path(__file__).resolve().parent

_RANK = {"R0": 0, "R1": 1, "R2": 2, "R3": 3}


class OpsxError(Exception):
    pass


def load_config() -> dict:
    cfg = {"readonly_patterns": [], "risk_rules": {"R1": [], "R2": [], "R3": []}}
    _parse_config(REPO_DIR / "config.yaml", cfg)
    override = STATE_DIR / "config.override.yaml"
    if override.exists():
        _parse_config(override, cfg)
    return cfg


def _parse_config(path: Path, cfg: dict) -> None:
    """极简行解析：顶层 `key:` 开段，`- value` 为条目，# 为注释。值为原始正则。"""
    section = None
    for raw in Path(path).read_text().splitlines():
        line = raw.rstrip()
        s = line.strip()
        if not s or s.startswith("#"):
            continue
        if not line.startswith((" ", "\t")) and s.endswith(":"):
            section = s[:-1]
            continue
        if s.startswith("- ") and section:
            item = s[2:].strip()
            if section == "readonly_patterns":
                cfg["readonly_patterns"].append(item)
            elif section in ("R1", "R2", "R3"):
                cfg["risk_rules"][section].append(item)


def classify(cmd: str) -> tuple[str, str | None]:
    """复合命令分段取最高风险级。返回 (级别, 命中的最高风险分段)。"""
    config = load_config()
    level, matched = "R0", None
    for seg in re.split(r"&&|\|\||[;|]", cmd):
        seg = seg.strip()
        if not seg:
            continue
        seg_level = _classify_single(seg, config)
        if _RANK[seg_level] > _RANK[level]:
            level, matched = seg_level, seg
    return level, matched


def _classify_single(seg: str, config: dict) -> str:
    for pat in config["readonly_patterns"]:
        if re.search(pat, seg):
            return "R0"
    for pat in config["risk_rules"]["R3"]:
        if re.search(pat, seg):
            return "R3"
    for pat in config["risk_rules"]["R2"]:
        if re.search(pat, seg):
            return "R2"
    for pat in config["risk_rules"]["R1"]:
        if re.search(pat, seg):
            return "R1"
    return "R2"  # 未知命令安全方向默认
```

- [ ] **Step 5: 运行测试，预期仅剩 stamp/check/snapshot/exec 相关失败**

Run: `python3 selftest.py`
Expected: `test_classify` PASS，其余 FAIL（函数未定义）

- [ ] **Step 6: 提交**

```bash
git add config.yaml core.py selftest.py
git commit -m "feat: 配置加载与命令风险分类（R0-R3）"
```

---

### Task 2: 审计日志

**Files:**
- Modify: `core.py`

**Interfaces:**
- Produces: `audit(event: str, **fields) -> None`——追加一行 JSON 到 `STATE_DIR/audit.jsonl`，行内含 `ts`(UTC ISO8601)。

- [ ] **Step 1: 测试已存在**（Task 1 的 `test_audit`）

- [ ] **Step 2: 实现 audit()，追加到 core.py**

```python
def audit(event: str, **fields) -> None:
    STATE_DIR.mkdir(parents=True, exist_ok=True)
    rec = {"ts": datetime.now(timezone.utc).isoformat(), "event": event, **fields}
    with open(STATE_DIR / "audit.jsonl", "a", encoding="utf-8") as f:
        f.write(json.dumps(rec, ensure_ascii=False) + "\n")
```

- [ ] **Step 3: 运行测试确认 test_audit 通过**

Run: `python3 -c "import selftest; raise SystemExit(selftest.main())" 2>/dev/null | grep test_audit`（或跑全量看进度）
Expected: `PASS test_audit`

- [ ] **Step 4: 提交**

```bash
git add core.py
git commit -m "feat: JSONL 审计日志"
```

---

### Task 3: 审批戳

**Files:**
- Modify: `core.py`

**Interfaces:**
- Produces:
  - `normalize(cmd: str) -> str`（压缩空白）
  - `cmd_hash(cmd: str) -> str`（sha256 hex）
  - `approve(cmd: str, ttl_seconds: int = 900, force: bool = False) -> Path`（R0 拒绝发戳、R3 必须 force；写 `approvals/<hash>.json`；写审计）
  - `check_stamp(cmd: str) -> bool`（存在且未过期）

- [ ] **Step 1: 测试已存在**（Task 1 的 `test_stamp`）

- [ ] **Step 2: 实现，追加到 core.py**

```python
def normalize(cmd: str) -> str:
    return " ".join(cmd.split())


def cmd_hash(cmd: str) -> str:
    return hashlib.sha256(normalize(cmd).encode()).hexdigest()


def approve(cmd: str, ttl_seconds: int = 900, force: bool = False) -> Path:
    level, _ = classify(cmd)
    if level == "R0":
        raise OpsxError("只读命令无需审批戳")
    if level == "R3" and not force:
        raise OpsxError("R3 命令需 --force 显式确认（不可逆操作）")
    stamp = {
        "cmd_hash": cmd_hash(cmd),
        "cmd": cmd,
        "created": time.time(),
        "ttl": ttl_seconds,
        "force": force,
        "level": level,
    }
    p = STATE_DIR / "approvals" / (stamp["cmd_hash"] + ".json")
    p.parent.mkdir(parents=True, exist_ok=True)
    p.write_text(json.dumps(stamp, ensure_ascii=False, indent=2), encoding="utf-8")
    audit("approve", cmd=cmd, level=level, ttl=ttl_seconds, force=force)
    return p


def check_stamp(cmd: str) -> bool:
    p = STATE_DIR / "approvals" / (cmd_hash(cmd) + ".json")
    if not p.exists():
        return False
    s = json.loads(p.read_text(encoding="utf-8"))
    return time.time() < s["created"] + s["ttl"]
```

- [ ] **Step 3: 运行确认 test_stamp 通过**

Run: `python3 selftest.py`
Expected: `PASS test_stamp`

- [ ] **Step 4: 提交**

```bash
git add core.py
git commit -m "feat: 审批戳（哈希+TTL+force）"
```

---

### Task 4: check() 组合逻辑（hook 核心）

**Files:**
- Modify: `core.py`

**Interfaces:**
- Produces: `check(cmd: str) -> tuple[int, str]`——(0, 放行原因) 或 (2, 拒绝原因含指引)。每次调用都写审计。

- [ ] **Step 1: 测试已存在**（Task 1 的 `test_check`）

- [ ] **Step 2: 实现，追加到 core.py**

```python
def check(cmd: str) -> tuple[int, str]:
    level, matched = classify(cmd)
    if level == "R0":
        audit("check", cmd=cmd, level=level, result="allow")
        return 0, "只读命令，放行"
    if check_stamp(cmd):
        audit("check", cmd=cmd, level=level, result="allow-stamp")
        return 0, "有效审批戳，放行"
    audit("check", cmd=cmd, level=level, result="deny")
    hit = f"（命中分段：{matched}）" if matched else ""
    return 2, (
        f"[opsx] 拒绝执行：该命令判定为 {level}{hit}。"
        "请先向用户提交变更单（风险说明+回滚计划），经确认后使用 opsx exec 执行。"
    )
```

- [ ] **Step 3: 运行确认 test_check 通过**

Run: `python3 selftest.py`
Expected: `PASS test_check`

- [ ] **Step 4: 提交**

```bash
git add core.py
git commit -m "feat: check() 白名单/审批戳判定与拒绝指引"
```

---

### Task 5: 快照与回滚

**Files:**
- Modify: `core.py`

**Interfaces:**
- Produces:
  - `snapshot_file(path_str: str) -> str`（返回 snapshot_id）
  - `snapshot_cmd(cmd: str, name: str = "") -> str`
  - `rollback(snapshot_id: str) -> str`（返回人读报告：恢复的文件 / 捕获的状态文本）
  - `prune_snapshots(keep: int = 200) -> None`
  - 内部（Task 6 复用）：`_begin_snapshot() -> tuple[str, Path]`、`_snapshot_file_into(d, path_str, items)`、`_snapshot_cmd_into(d, cmd, name, items)`、`_write_meta(d, sid, items)`

快照目录结构：`~/.ops-agent/snapshots/<id>/meta.json` + `files/<绝对路径去前导/>` + `cmd-<name>.txt`。

- [ ] **Step 1: 测试已存在**（Task 1 的 `test_snapshot_file_rollback`、`test_snapshot_cmd`）

- [ ] **Step 2: 实现，追加到 core.py**

```python
def _new_snapshot_id() -> str:
    return datetime.now().strftime("%Y%m%d-%H%M%S") + "-" + uuid.uuid4().hex[:6]


def _begin_snapshot() -> tuple[str, Path]:
    sid = _new_snapshot_id()
    d = STATE_DIR / "snapshots" / sid
    d.mkdir(parents=True, exist_ok=True)
    return sid, d


def _snapshot_file_into(d: Path, path_str: str, items: list) -> None:
    src = Path(path_str)
    if not src.is_file():
        raise OpsxError(f"快照源不是文件: {path_str}")
    dest = d / "files" / str(src.resolve()).lstrip("/")
    dest.parent.mkdir(parents=True, exist_ok=True)
    shutil.copy2(src, dest)
    items.append({
        "type": "file",
        "src": str(src.resolve()),
        "dest": str(dest.relative_to(d)),
        "mode": stat.S_IMODE(src.stat().st_mode),
    })


def _snapshot_cmd_into(d: Path, cmd: str, name: str, items: list) -> None:
    r = subprocess.run(cmd, shell=True, capture_output=True, text=True, timeout=60)
    if r.returncode != 0:
        raise OpsxError(f"状态抓取命令失败({r.returncode}): {cmd}\n{r.stderr[:500]}")
    fname = f"cmd-{(name or 'output').replace('/', '_')}.txt"
    (d / fname).write_text(r.stdout, encoding="utf-8")
    items.append({"type": "cmd", "cmd": cmd, "name": name, "output": fname})


def _write_meta(d: Path, sid: str, items: list) -> None:
    meta = {"id": sid, "created": datetime.now(timezone.utc).isoformat(), "items": items}
    (d / "meta.json").write_text(json.dumps(meta, ensure_ascii=False, indent=2), encoding="utf-8")


def snapshot_file(path_str: str) -> str:
    sid, d = _begin_snapshot()
    items: list = []
    _snapshot_file_into(d, path_str, items)
    _write_meta(d, sid, items)
    audit("snapshot", id=sid, kind="file", src=path_str)
    return sid


def snapshot_cmd(cmd: str, name: str = "") -> str:
    sid, d = _begin_snapshot()
    items: list = []
    _snapshot_cmd_into(d, cmd, name, items)
    _write_meta(d, sid, items)
    audit("snapshot", id=sid, kind="cmd", cmd=cmd)
    return sid


def rollback(snapshot_id: str) -> str:
    d = STATE_DIR / "snapshots" / snapshot_id
    if not d.exists():
        raise OpsxError(f"快照不存在: {snapshot_id}")
    meta = json.loads((d / "meta.json").read_text(encoding="utf-8"))
    report = []
    for item in meta["items"]:
        if item["type"] == "file":
            shutil.copy2(d / item["dest"], item["src"])
            os.chmod(item["src"], item["mode"])
            report.append(f"已恢复文件: {item['src']}")
        else:
            out = (d / item["output"]).read_text(encoding="utf-8")
            report.append(f"捕获状态 [{item.get('name') or item['cmd']}]:\n{out}")
    audit("rollback", snapshot_id=snapshot_id)
    return "\n".join(report)


def prune_snapshots(keep: int = 200) -> None:
    d = STATE_DIR / "snapshots"
    if not d.exists():
        return
    ids = sorted(p for p in d.iterdir() if p.is_dir())
    for p in ids[:-keep]:
        shutil.rmtree(p)
        audit("prune", snapshot_id=p.name)
```

- [ ] **Step 3: 运行确认两个 snapshot 测试通过**

Run: `python3 selftest.py`
Expected: `PASS test_snapshot_cmd`、`PASS test_snapshot_file_rollback`

- [ ] **Step 4: 提交**

```bash
git add core.py
git commit -m "feat: 快照与回滚（文件+状态捕获，保留200个）"
```

---

### Task 6: exec_change() 原子执行

**Files:**
- Modify: `core.py`

**Interfaces:**
- Produces: `exec_change(cmd: str, snapshot_files: list[str] | None = None, snapshot_cmds: list[tuple[str, str]] | None = None) -> int`——校验（check）→ 快照（统一 snapshot_id，失败即清理并返回 3）→ 执行 → 审计 + 清理。返回命令退出码或 2/3。

- [ ] **Step 1: 测试已存在**（Task 1 的 `test_exec_aborts_on_snapshot_failure`、`test_exec_happy`）

- [ ] **Step 2: 实现，追加到 core.py**

```python
def exec_change(
    cmd: str,
    snapshot_files: list[str] | None = None,
    snapshot_cmds: list[tuple[str, str]] | None = None,
) -> int:
    snapshot_files = snapshot_files or []
    snapshot_cmds = snapshot_cmds or []
    code, msg = check(cmd)
    if code != 0:
        print(msg, file=sys.stderr)
        return code
    sid = None
    if snapshot_files or snapshot_cmds:
        sid, d = _begin_snapshot()
        items: list = []
        try:
            for f in snapshot_files:
                _snapshot_file_into(d, f, items)
            for c, name in snapshot_cmds:
                _snapshot_cmd_into(d, c, name, items)
        except OpsxError as e:
            shutil.rmtree(d, ignore_errors=True)
            print(f"[opsx] 快照失败，已中止执行: {e}", file=sys.stderr)
            audit("exec-abort", cmd=cmd, reason=str(e))
            return 3
        _write_meta(d, sid, items)
    r = subprocess.run(cmd, shell=True)
    audit("exec", cmd=cmd, snapshot_id=sid, exit_code=r.returncode)
    prune_snapshots()
    return r.returncode
```

- [ ] **Step 3: 运行全量 selftest，确认全绿**

Run: `python3 selftest.py`
Expected: `8/8 passed`

- [ ] **Step 4: 提交**

```bash
git add core.py
git commit -m "feat: exec 原子执行（校验→快照→执行→审计）"
```

---

### Task 7: opsx CLI（含 hook 子命令）

**Files:**
- Create: `opsx`

**Interfaces:**
- Consumes: core 全部公开函数（`check/approve/exec_change/snapshot_file/snapshot_cmd/rollback/STATE_DIR`）。
- Produces: 可执行入口 `./opsx <子命令>`；`opsx hook` 从 stdin 读 Claude Code hook JSON，取 `tool_input.command` 做 check，exit 0 放行 / exit 2 拒绝（任何异常也 exit 2）；`opsx selftest` 跑 selftest.main()。

- [ ] **Step 1: 写 opsx**

```python
#!/usr/bin/env python3
"""opsx CLI——core.py 的薄壳。"""
import argparse
import json
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent))
import core  # noqa: E402


def _join(parts: list[str]) -> str:
    return " ".join(parts)


def main(argv: list[str] | None = None) -> int:
    p = argparse.ArgumentParser(prog="opsx", description="运维变更守门：分类/审批戳/快照/回滚/审计")
    sub = p.add_subparsers(dest="sub", required=True)

    c = sub.add_parser("check", help="hook 入口：判定命令并输出放行/拒绝")
    c.add_argument("command", nargs="+")

    a = sub.add_parser("approve", help="手动预授权（降级路径）")
    a.add_argument("command", nargs="+")
    a.add_argument("--ttl", type=int, default=900, help="有效期秒数")
    a.add_argument("--force", action="store_true", help="R3 不可逆操作必须显式加此 flag")

    e = sub.add_parser("exec", help="原子执行：校验→快照→执行→审计")
    e.add_argument("command", nargs="+")
    e.add_argument("--snapshot-file", action="append", default=[])
    e.add_argument("--snapshot-cmd", action="append", default=[],
                   help="'name::command' 形式，可多次")

    s = sub.add_parser("snapshot", help="独立快照")
    g = s.add_mutually_exclusive_group(required=True)
    g.add_argument("--file")
    g.add_argument("--cmd")
    s.add_argument("--name", default="")

    r = sub.add_parser("rollback")
    r.add_argument("snapshot_id")

    sub.add_parser("list", help="列出快照与审批戳")

    at = sub.add_parser("audit")
    at.add_argument("--tail", type=int, default=20)

    sub.add_parser("selftest", help="运行内置自检")

    sub.add_parser("hook", help="Claude Code PreToolUse hook 入口（stdin 读 JSON）")

    args = p.parse_args(argv)

    try:
        if args.sub == "hook":
            try:
                data = json.load(sys.stdin)
                command = data.get("tool_input", {}).get("command", "")
                code, msg = core.check(command)
                if code != 0:
                    print(msg, file=sys.stderr)
                return code
            except Exception as e:  # noqa: BLE001 —— hook 异常一律拒绝方向
                print(f"[opsx] hook 异常，拒绝执行: {e}", file=sys.stderr)
                return 2

        if args.sub == "check":
            code, msg = core.check(_join(args.command))
            print(msg, file=sys.stderr if code else sys.stdout)
            return code

        if args.sub == "approve":
            path = core.approve(_join(args.command), ttl_seconds=args.ttl, force=args.force)
            print(f"已授权（{path.name}，TTL {args.ttl}s）")
            return 0

        if args.sub == "exec":
            snap_cmds = []
            for spec in args.snapshot_cmd:
                name, _, cmd = spec.partition("::")
                snap_cmds.append((cmd, name))
            return core.exec_change(_join(args.command), args.snapshot_file, snap_cmds)

        if args.sub == "snapshot":
            sid = core.snapshot_file(args.file) if args.file else core.snapshot_cmd(args.cmd, args.name)
            print(sid)
            return 0

        if args.sub == "rollback":
            print(core.rollback(args.snapshot_id))
            return 0

        if args.sub == "list":
            lines = []
            sdir = core.STATE_DIR / "snapshots"
            if sdir.exists():
                lines.append("== snapshots ==")
                lines += [x.name for x in sorted(sdir.iterdir()) if x.is_dir()]
            adir = core.STATE_DIR / "approvals"
            if adir.exists():
                lines.append("== approvals ==")
                for x in sorted(adir.glob("*.json")):
                    st = json.loads(x.read_text(encoding="utf-8"))
                    lines.append(f"{st['cmd'][:80]}  (level={st['level']}, ttl={st['ttl']}s)")
            print("\n".join(lines) or "(空)")
            return 0

        if args.sub == "audit":
            f = core.STATE_DIR / "audit.jsonl"
            if not f.exists():
                print("(无审计日志)")
                return 0
            lines = f.read_text(encoding="utf-8").strip().splitlines()
            for line in lines[-args.tail:]:
                print(line)
            return 0

        if args.sub == "selftest":
            import selftest
            return selftest.main()

    except core.OpsxError as e:
        print(f"[opsx] 错误: {e}", file=sys.stderr)
        return 1


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 2: 赋可执行权限并跑冒烟**

Run:
```bash
chmod +x opsx
echo '{"tool_input":{"command":"docker ps"}}' | ./opsx hook && echo HOOK_ALLOW
echo '{"tool_input":{"command":"systemctl restart nginx"}}' | ./opsx hook; echo "exit=$?"
./opsx selftest
```
Expected: `HOOK_ALLOW`；`exit=2` 且 stderr 含变更单指引；selftest `8/8 passed`。

- [ ] **Step 3: 提交**

```bash
git add opsx
git commit -m "feat: opsx CLI（含 Claude Code hook 入口）"
```

---

### Task 8: Agent 定义、hook 配置与 README

**Files:**
- Create: `.claude/agents/ops.md`
- Create: `.claude/settings.json`
- Create: `README.md`

**Interfaces:**
- Consumes: `./opsx` 各子命令（Task 7）。
- Produces: 在 `ops-agent/` 目录内启动 `ops` agent 时生效的完整防线。

- [ ] **Step 1: 写 .claude/agents/ops.md**

```markdown
---
name: ops
description: 运维操作代理。默认只读；任何写操作必须按 R1/R2/R3 流程提交变更单并经用户确认后通过 opsx exec 执行。适用于服务状态查看、日志排查、部署变更、故障处理等运维场景。
tools: Bash, Read, Grep, Glob, AskUserQuestion
---

你是运维 Agent，负责本机 macOS、远程 Linux 服务器（SSH 直连）、K8s 集群（kubeconfig 直连）、Docker 环境的运维操作。

## 最高原则

1. **默认只读**：优先用只读命令查状态、看日志、定位问题。写操作是例外，不是默认。
2. **所有写命令必须经 opsx exec 执行**，禁止直接 Bash 执行写操作。hook 会拦截未授权命令。
3. **禁止拆分命令规避审批**、禁止在复合命令里夹带未申报的命令段。

## 风险分级

- **R0 只读**：kubectl get/describe/logs、systemctl status、docker ps/logs、日志检索等。直接执行。
- **R1 可逆低风险写**：服务重启、docker restart、改有快照的非关键配置。
- **R2 高风险写**：部署/升级、扩缩容、关键配置变更、包安装、防火墙规则。
- **R3 不可逆**：rm -rf、kubectl delete、docker rm/rmi、格式化、DROP 等。**默认拒绝**。

判定依据 `opsx check '<命令>'` 的返回；不确定时按更高级别处理。

## R1/R2 强制流程

1. 输出**变更单**，包含：目标环境、完整命令、风险说明（为何此级别、影响面）、回滚计划、快照计划。
2. 用 AskUserQuestion 请用户确认。用户拒绝则终止并解释。
3. 用户批准后执行（快照参数随命令一次传入，保证原子性）：
   `opsx exec '<命令>' --snapshot-file <路径> --snapshot-cmd 'name::<抓取当前状态的命令>'`
4. 执行后报告：结果、snapshot_id、`opsx rollback <snapshot_id>` 回滚命令。
   服务/部署类回滚：先 `opsx rollback <id>` 取回执行前状态，再按回滚计划执行逆操作。

## R3 流程

默认拒绝并向用户说明风险。仅当用户在会话中显式输入确认（如明确回复"确认执行"）后才可执行，且变更单必须注明"此操作不可逆，无回滚方案"。`opsx exec` 前必须先 `opsx approve '<命令>' --force`。

## 报告义务

每次变更执行后必须报告：执行的命令、退出码、snapshot_id、回滚命令。审计日志由 opsx 自动写入。

## 输出

- 默认使用中文。
- 只读排查时先给结论再给证据（命令+关键输出摘要）。
```

- [ ] **Step 2: 写 .claude/settings.json**

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"$CLAUDE_PROJECT_DIR/opsx\" hook",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
```

- [ ] **Step 3: 写 README.md**

```markdown
# ops-agent

默认只读、高风险操作需会话内授权并附风险说明与快照回滚的 Claude Code 运维子代理。

## 组成

- `.claude/agents/ops.md` — Agent 定义（R0-R3 风险分级与流程纪律）
- `.claude/settings.json` — PreToolUse hook，对所有 Bash 命令执行 `opsx check`
- `opsx` / `core.py` — 纯标准库 CLI/核心库（分类、审批戳、快照、回滚、审计）
- `config.yaml` — 只读白名单与风险规则；`~/.ops-agent/config.override.yaml` 追加覆盖

## 使用

必须在 `ops-agent/` 目录内启动（hook 为项目级配置，目录外只有 prompt 约束一层防线）。

```bash
# 自检
./opsx selftest

# 手动预授权（AskUserQuestion 不可用时的降级路径）
./opsx approve 'systemctl restart nginx' --ttl 900

# 查看审计 / 快照 / 审批戳
./opsx audit --tail 20
./opsx list
./opsx rollback <snapshot_id>
```

## 防线说明

两层防线：① Agent 流程纪律（变更单 → AskUserQuestion → opsx exec）；
② PreToolUse hook 硬拦截（无白名单命中且无有效审批戳 → exit 2 拒绝）。

已知边界：审批戳由 Agent 侧在获得用户确认后创建，防"误执行"不防"恶意 Agent"；
命令分类为线性正则匹配，不解析 shell AST（复合命令按 `;` `&&` `||` `|` 分段取最高级）。

## 演进

core.py 为纯函数库；演进 MCP server 时直接包装同一组函数，状态目录布局即对外契约。
```

- [ ] **Step 4: 端到端冒烟**

Run（在 ops-agent/ 目录内）:
```bash
./opsx selftest
echo '{"tool_input":{"command":"kubectl get pods"}}' | ./opsx hook; echo "read=$?"
```
Expected: `8/8 passed`；`read=0`。

- [ ] **Step 5: 提交**

```bash
git add .claude/agents/ops.md .claude/settings.json README.md
git commit -m "feat: ops agent 定义、hook 配置与使用文档"
```

---

## Self-Review 记录

- 规格覆盖：分级模型(T1)、复合命令最高级(T1)、审计(T2)、审批戳+TTL+force(T3)、check 兜底(T4)、快照/回滚/保留200(T5)、exec 原子中止(T6)、CLI+hook 异常拒止(T7)、agent 定义/hook 配置/降级路径 README(T8)。spec 4.6 降级路径（`! opsx approve`）写入 README 与 agent 提示词。
- 无占位符；接口名跨任务一致（classify/check/approve/check_stamp/snapshot_file/snapshot_cmd/rollback/exec_change/audit/prune_snapshots）。
- 与 spec 的一处偏差说明：spec 第 5 节 CLI 表中 `list [--snapshots|--approvals]` 简化为 `list`（两者同列）；`audit [--tail N]` 保留。
